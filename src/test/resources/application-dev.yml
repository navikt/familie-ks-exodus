app:
  frontend:
    path: "frontend/build"
  gruppe:
    admin: "gruppe-admin"
  tilganger:
    testtabeller: default

spring:
  h2:
    console.enabled: true
  datasource:
    hikari:
      schema: test
  jpa:
    hibernate:
      ddl-auto: validate

no.nav.security.jwt:
  expirythreshold: 60 #threshold in minutes until token expires
  issuer:
    azure:
      discoveryurl: "http://host.docker.internal:8102/azure-mock/.well-known/openid-configuration"
      #todo oppdater navn
      accepted_audience: historisk-tidsbegrenset-uforestonad
      proxyurl: ~
